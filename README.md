# Simple React seconds counter.

Count seconds starting on page load.

## How to use:

Constant ```counterSize``` in ```./src/js/index.js``` define que ammount of digits in the counter. Set it.

```bash
npm install
npm start
```
